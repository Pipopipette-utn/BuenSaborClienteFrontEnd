export enum FormaPago {
    EFECTIVO,
    MERCADO_PAGO
}

export enum Estado {
    PREPARACION,
    PENDIENTE,
    CANCELADO,
    RECHAZADO,
    ENTREGADO
}


export enum Rol {
    ADMIN,
    CLIENTE,
    COCINERO,
    CAJERO,
    DELIVERY
}

export enum TipoEnvio {
    DELIVERY,
    TAKE_AWAY
}


export enum TipoPromocion {
    HAPPY_HOUR,
    PROMOCION
}